import React from 'react'
import { Container } from 'react-bootstrap'

const ServerInfo = () => {
    return (
        <Container>
            ServerInfo
        </Container>
    )
}

export default ServerInfo