import React from "react";
import { Container } from "react-bootstrap";

const WebSocket = () => {
    return (
        <Container>
            WebSocket
        </Container>
    )
}

export default WebSocket